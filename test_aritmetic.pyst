start() {
    var a: float;
    var c: float;
    var b: float;
    var k: float;
    function jeje(var z: int;): int {
        a=k*c;
        return 5;
    };

    function main(): void {
        var lol: float;
        var sos: float;
        var ja: bool;
        ja=True;
        c=5+(a+k+b+5)*sos;
    };
    k=b+c;
    jeje(3);
    jeje(6+7);
}