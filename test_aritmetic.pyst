start() {
    var a: float;
    var c: float;
    var b: float;
    var k: float;
    var res: int;
    function jeje(var z: int;): int {
        a=k*c;
        return 5;
    };

    function main(): void {
        var lol: float;
        var sos: float;
        var ja: bool;
        ja=True;
        c=5+(a+k+b+5)*sos;
        print(jeje(3*res));
    };
    k=b+c;
    res = jeje(3)*jeje(6+7);
}