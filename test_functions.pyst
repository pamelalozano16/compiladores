start() {
    var a: int;
    var b: int;

    function jeje(var z: int;): int {
        var res: int;
        res = z*5;
        return res;
    };

    function main(): void {
        var c:int;
        var k:int;
        b=3;
        c=9;
        k=jeje(2*4+5);
        print(jeje(3), jeje(4), k);
    };
}